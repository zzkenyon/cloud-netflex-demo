spring:
  profiles:
    active: dev
---
spring:
  profiles: prod
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:6001
      label: master
      profile: prod

---
spring:
  profiles: dev
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:6001
      label: master
      profile: dev
